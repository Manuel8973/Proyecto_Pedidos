@{
    ViewData["Title"] = "Área de Clientes";
}

<div class="text-center">
    <div class="alert alert-info">
    <h3><i class="bi bi-shield-check"></i> ¡Acceso Correcto!</h3>
      <p>Solo los <strong>Clientes</strong> pueden ver esta página.</p>
    <p>Usuario: <strong>@User.Identity.Name</strong></p>
     <p>Rol: <strong>@User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value</strong></p>
 </div>
   
    <a asp-action="Index" class="btn btn-primary">Volver a Pruebas</a>
</div>